_site/
.netlify/
__pycache__/
*.pyc
node_modules/
.build-tools/
tracking/samples/*.mp4
tracking/samples/*.webm
tracking/samples/*.insv
tracking/outputs/
tracking/.venv/
*.pt
.DS_Store

# post_game runtime artifacts
post_game/.cache/
post_game/outputs/
post_game/models/
.venv-post-game/

# local secrets for run_analytics.sh (R2 keys, etc.)
.env
.env.local
dist/
wipe_all_games.py

# Local tooling / build cache
.claude/
worker/.wrangler/

# Raw game-day audio (large; kept locally, not in history)
voice memo/
post_game/raw/voice/
tracking/labels/

# rendered click-sampling frames (large, regenerable from the raw video)
tracking/outputs/click_samples/

# click-sampler run log
run*.log

# local parent contact data (never commit — kids/parents PII)
parent_contacts.local.json

# TeamSnap feed snapshots used by scripts/calendar_model_check.mjs. Not secrets
# (the feed URL is the credential and lives in a Worker secret), but they record
# where the kids will be and when — same reason parent_contacts.local.json is
# ignored. Re-fetch locally when you need one.
test/fixtures/*.ics

# workbench narration media hardlinks (86GB reels — never commit)
workbench/media/

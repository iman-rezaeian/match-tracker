# Core
# numpy pinned to a wheel that exists in our corporate artifactory mirror
# (the proxy has a /packages/packages/ 404 bug on most newer wheels).
numpy==1.26.4
pandas>=2.2
opencv-python>=4.10
pillow>=10

# Detection + tracking
ultralytics>=8.3        # YOLO11
boxmot>=10.0.77         # BoT-SORT with OSNet Re-ID
torch>=2.3
torchvision>=0.18

# Identity helpers (low priority, optional)
paddleocr>=2.7
paddlepaddle>=2.6
insightface>=0.7.3
onnxruntime>=1.18

# Stats + viz
scikit-learn>=1.5
scipy>=1.13
matplotlib>=3.8
mplsoccer>=1.4

# Firestore + R2
# Pinned upper bounds avoid uncached wheels in corporate artifactory mirror.
google-cloud-firestore>=2.16,<3
google-cloud-core<2.6
pyasn1-modules<0.4.2
proto-plus<1.28
google-auth>=2.30
boto3>=1.34              # S3 API for Cloudflare R2

# CLI / utils
typer>=0.12
rich>=13.7
tqdm>=4.66
streamlit>=1.35          # local UI: `streamlit run post_game/ui_app.py`
